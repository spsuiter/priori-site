/**
 * Priori Site Configuration
 * ─────────────────────────
 * Edit this file when deploying for a new firm.
 * All contact form submissions will route to CONTACT_EMAIL.
 */

var PRIORI_CONFIG = {
  // Contact email — change this for each firm deployment
  CONTACT_EMAIL: ['sean', 'suiter.com'],   // assembled as [0] + '@' + [1]

  // Firm name shown in page title / meta (optional, future use)
  FIRM_NAME: 'Priori'
};
